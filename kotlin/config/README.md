# Config template for DEV environment specific settings and overwrites

* `**/config/application.properties` is .gitignored, create a local copy from the template and replace the placeholders with values, but **don't** put under version control
* `**/config/application.properties.tmpl` is versioned and used as template, but must **not** contain any sensitive values!

