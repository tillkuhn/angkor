{
  "name": "json-server",
  "version": "0.0.2",
  "scripts": {
    "server": "json-server --port 8080 --watch db.json --routes routes.json",
    "server:delay": "json-server --delay 2000 --port 8080 --watch db.json --routes routes.json"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
  }
}
